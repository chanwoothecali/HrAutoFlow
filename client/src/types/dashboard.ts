export type DashboardStats = {
  new: number;
  inProgress: number;
  finalInterview: number;
  hired: number;
  pending: number;
};

export type PositionPieDatum = {
  name: string;
  value: number;
};
